namespace eval vhdl-types {
  proc src {} {
    hbs::SetLib "ltypes"
    hbs::AddFile types.vhd
  }

  hbs::Register "Collection of useful VHDL types that are frequently used, but are not part of the standard."
}